version: 2
updates:
  - package-ecosystem: docker
    directory: "/"
    schedule:
      interval: weekly
      day: monday
    labels:
      - "dependencies"
      - "docker"
    ignore:
      - dependency-name: "*"
        versions: ["*a*", "*b*", "*rc*", "*alpha*", "*beta*"]
